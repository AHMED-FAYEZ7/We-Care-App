import 'package:flutter/material.dart';

class HistoryPatientScreen extends StatelessWidget {
  const HistoryPatientScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('history'),
    );
  }
}
