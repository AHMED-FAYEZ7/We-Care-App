import 'package:flutter/material.dart';

class PostsDoctorScreen extends StatelessWidget {
  const PostsDoctorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "PostsDoctorScreen",
      ),
    );
  }
}
