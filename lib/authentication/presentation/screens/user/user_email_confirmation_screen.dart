import 'package:flutter/material.dart';

class UserEmailConfirmationScreen extends StatelessWidget {
  const UserEmailConfirmationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
