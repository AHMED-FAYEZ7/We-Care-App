import 'package:flutter/material.dart';

class UserForgetPasswordScreen extends StatelessWidget {
  const UserForgetPasswordScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
